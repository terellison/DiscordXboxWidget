<Page
    x:Class="DiscordWidget.SettingsWidget"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DiscordWidget"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="12" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Discord application"
                   Style="{StaticResource SubtitleTextBlockStyle}" />

        <TextBlock Grid.Row="1"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                   TextWrapping="Wrap">
            <Run Text="Create an app at discord.com/developers/applications, enable" />
            <Run Text="Public Client" FontWeight="SemiBold" />
            <Run Text="on its OAuth2 tab, add http://localhost as a redirect URI, then paste the Application ID here." />
        </TextBlock>

        <TextBox Grid.Row="2"
                 x:Name="ClientIdBox"
                 Header="Application ID"
                 PlaceholderText="e.g. 123456789012345678"
                 InputScope="Number"
                 TextChanged="OnClientIdChanged" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8">
            <Button x:Name="SaveButton"
                    Content="Save and connect"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="OnSave"
                    IsEnabled="False" />
            <ProgressRing x:Name="Busy" Width="20" Height="20" IsActive="False" />
        </StackPanel>

        <TextBlock Grid.Row="4"
                   x:Name="StatusText"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   TextWrapping="Wrap"
                   Margin="0,4,0,0" />
    </Grid>
</Page>
